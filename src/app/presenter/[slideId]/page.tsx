export default function PresenterBasePage() {
	// This page corresponds to the implicit `children` slot in layout.tsx
	// We render null because the presenter UI is handled by parallel routes.
	return null;
}
